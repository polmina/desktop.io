import { keyframes } from "styled-components";
export const blob = keyframes`
  0% {
    transform: scale(0) inherit;
  }
  50% {
    transform: scale(1.5) inherit;
  }
  100% {
    transform: scale(1) inherit;
  }
`;
